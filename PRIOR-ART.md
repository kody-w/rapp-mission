# Prior art

This file is a dated public record. The commit that adds it, and every commit after, is
timestamped by Git and GitHub, and the tag `prior-art-2026-09-04` anchors the first statement.

## 2026-09-04

Kody Wildfeuer states the direction in these terms, recorded the same day:

- Skills are the product. A single `SKILL.md` is all a user needs. It carries its own code.
- The user's one file is their whole AI: identity, preferences, work, what they have taught it. It
  moves from AI to AI and device to device unchanged, and each AI "just knows them".
- The file is the organism on top of the bones of any native platform. It rides on what is native
  and never presents a second system beside it.
- Consistent experience across platforms is the key: the same file, the same behavior, the same
  feel, everywhere.
- No jargon to users, only capabilities. No installs, no accounts, no terms to learn.
- Deleting the file removes the whole layer and returns the AI to its native baseline, with no
  residue.
- Effort shifts away from building harnesses, runtimes, or interfaces. The value is the file being
  absorbed by native platforms and making them better, invisibly.
- The Brainstem, the project's own local runtime, is the last-case scenario: it runs the same
  file, only if everything native has failed. It is never required.
- Every capability converts losslessly between a skill file and one Python file, so the same thing
  runs in a chat tool, on a server, or in a team's runtime without a rewrite.
- "Let everyone else commoditize in their bubbles. RAPP is the ark for the user to stay in control
  and command wherever needed. That is RAPP/1's purpose."
- "The harness at this point is just causing confusion."
- "RAPP is a state of mind more than anything."

First working implementation, verified live on Claude Code 2.1.260 and GitHub Copilot CLI
1.0.83 with nothing installed, the same day: https://github.com/kody-w/rapp-skills.
The previous implementation of the same idea from August 2026 was archived as
https://github.com/kody-w/rapp-skills-legacy.
